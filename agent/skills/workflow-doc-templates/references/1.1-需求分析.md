# {算子名称} 需求分析

> 1.1 需求分析阶段产出。核对确认项，用户未提及的通过问卷补齐。作为后续 Spec、方案、测试的输入。

## 1. cuSPARSE 对标信息

| 项目 | 内容 |
|------|------|
| 对标接口 | {cusparse 对标接口名} |
| 接口文档链接 | {cuSPARSE 文档 URL} |
| API 体系 | {Generic API / Legacy API} |
| 支持的稀疏格式 | {CSR / COO / CSC / ...} |
| 支持的数据类型 | {FP32 / FP16 / BF16 / INT32 / ...} |

## 2. 数学定义

```
// 输入输出
输入: {A - 稀疏矩阵, shape, dtype; x - 稠密向量, shape, dtype}
输出: {y - 稠密向量, shape, dtype}

// 数学公式
{如: y = alpha * op(A) * x + beta * y}
```

### 操作说明

| 参数 | 说明 |
|------|------|
| A | 稀疏矩阵 |
| x | 输入稠密向量 |
| y | 输出稠密向量 |
| alpha | 标量 |
| beta | 标量 |
| op | 操作类型（非转置/转置/共轭转置） |

## 3. 算子规格

### 3.1 基本信息

| 项目 | 内容 |
|------|------|
| 算子名称 | {aclsparseXxx} |
| 算子类别 | {Sparse Matrix-Vector / Sparse Matrix-Matrix / Tridiagonal Solve / ...} |
| 需求类型 | {特定用例（指定 shape/dtype） / 通用} |
| 代码目录 | `sparse/{op_name}/` |
| 测试目录 | `test/{op_name}/` |

### 3.2 接口原型

```c
aclsparseStatus_t aclsparse{Xxx}(
    aclsparseHandle_t handle,
    {参数列表}
);
```

### 3.3 参数规格

| 参数 | 内存位置 | 方向 | 类型 | 说明 |
|------|----------|------|------|------|
| handle | Host | 输入 | aclsparseHandle_t | 库上下文句柄 |
| {param} | {Host/Device} | {输入/输出} | {类型} | {说明} |

### 3.4 参数约束

| 参数 | 约束 |
|------|------|
| handle | 非 nullptr |
| matA.rows | ≥ 0 |
| matA.cols | ≥ 0 |
| matA.nnz | ≥ 0 |
| alpha | Host 指针 |

### 3.5 支持数据类型

| 计算类型 | 值类型 | 输出类型 | 是否支持 |
|----------|-------|---------|---------|
| FP32 | FP32 | FP32 | |
| FP32 | FP16 | FP16 | |
| FP32 | BF16 | BF16 | |

### 3.6 稀疏格式

| 格式 | 是否支持 | 说明 |
|------|---------|------|
| CSR | | |
| COO | | |
| CSC | | |

### 3.7 目标芯片与架构

| 项目 | 内容 |
|------|------|
| 目标芯片 | {芯片型号} |
| 目标架构 | {archXX} |
| CANN 版本 | {版本} |

### 3.8 精度要求

| dtype | rtol | atol | 说明 |
|-------|------|------|------|
| FP32 | 1e-5 | 1e-5 | FP32 标准 |
| FP16 | 1e-3 | 1e-3 | FP16 标准 |
| BF16 | 5e-3 | 5e-3 | BF16 标准 |
| INT32 | 位精确 | 位精确 | 整数计算 |

### 3.9 性能预期

{性能目标或"无明确要求"}

## 4. 可行性评估

### 4.1 API 可用性

{Ascend C API 可用性分析}

### 4.2 性能可行性

{预估的 AI Core 利用率和性能瓶颈}

### 4.3 风险点

| 风险 | 影响 | 缓解措施 |
|------|------|---------|

## 5. 其他要求

{用户指定的实现方案约束，如要求 SIMT 实现；无则填"无"}

## 6. 用户原始需求

<!-- 逐条记录，不加工、不裁剪，作为下游对照的根本依据 -->

| # | 需求内容 |
|---|----------|
| 1 | |
