# PR-Issue 关联问卷

**触发条件**：用户只提到提 PR 时

```
问题：是否需要同时创建一个关联的 Issue？
选项：
- 是，同步创建新 Issue（推荐）
- 不需要，我已有 Issue（用户在自定义输入中提供 Issue 编号）
- 不需要 Issue，只提 PR
```

**后续处理**：
- 选择"同步创建"：先走 Issue 模板流程（步骤 2-6），再走 PR 流程，PR 正文中自动关联新创建的 Issue 编号
- 选择"已有 Issue"：PR 正文中关联用户提供的 Issue 编号
- 选择"不需要"：直接走 PR 流程
